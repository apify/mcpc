#!/usr/bin/env node

/**
 * Example MCP server for testing mcpc package resolution
 * This is a minimal mock server that demonstrates the package resolution feature
 */

console.log('Example MCP server starting...');
console.log('This is a test package to demonstrate mcpc package resolution');
console.log('In a real MCP server, this would handle MCP protocol communication via stdio');
process.exit(0);
