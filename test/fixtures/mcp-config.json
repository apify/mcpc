{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest"]
    },

    "fs": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "${PWD}"]
    },

    "apify-docs": {
      "url": "https://mcp.apify.com/?tools=docs"
    },

    "apify-bearer": {
      "url": "mcp.apify.com/#blabla",
      "headers": {
        "Authorization": "Bearer ${APIFY_TOKEN}"
      }
    }
  }
}
